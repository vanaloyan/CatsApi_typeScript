import styled from "styled-components";
import { COLORS } from "../../../utils";

export const active = styled.a`
  background-color: ${COLORS.xRed};
`;

export const isActive = styled.a`
  background-color: ${COLORS.xRed};
`;
